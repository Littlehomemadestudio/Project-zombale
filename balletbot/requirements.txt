# BalletBot: Outbreak Dominion Requirements
# Core dependencies
bale-python>=1.0.0  # Bale messenger API
Pillow>=9.0.0       # Image processing for maps
asyncio-mqtt>=0.11.0  # Optional: for advanced messaging

# Standard library dependencies (included with Python 3.10+)
# sqlite3 - built-in
# json - built-in
# logging - built-in
# pathlib - built-in
# dataclasses - built-in
# typing - built-in
# time - built-in
# random - built-in
# math - built-in

# Development dependencies (optional)
pytest>=7.0.0       # For testing
pytest-asyncio>=0.21.0  # For async testing